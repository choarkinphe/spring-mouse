# Repository Agent Instructions

## Release versioning

- Before every `git push`, increment the root package version by `0.0.1` (a patch release) and keep the lockfile version synchronized.
- If the user explicitly requests a major, minor, or specific version upgrade, follow that request instead.
